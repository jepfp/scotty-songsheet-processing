package ch.scotty.job.json

case class SingleSongToImageConverterJobConfiguration(
                                                       songId: Long
                                                     )
