scotty-mole-integration-testing-dbsetup = {
  dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
  properties = {
      url = "jdbc:mysql://localhost:3306/?user=root&password=changeme&serverTimezone=Europe/Zurich"
      driver = slick.jdbc.MySQLProfile
  }
}

scotty-mole-integration-testing = {
    dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
    properties = {
        url = "jdbc:mysql://localhost:3306/scottymole_inttest?user=root&password=changeme&serverTimezone=Europe/Zurich"
        driver = slick.jdbc.MySQLProfile
    }
}

songanize-integration-testing-dbsetup = {
    dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
    properties = {
        url = "jdbc:mysql://localhost:3306/?user=root&password=changeme&serverTimezone=Europe/Zurich"
        driver = slick.jdbc.MySQLProfile
    }
}

songanize-integration-testing = {
    dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
    properties = {
        url = "jdbc:mysql://localhost:3306/songanize_inttest?user=root&password=changeme&serverTimezone=Europe/Zurich"
        driver = slick.jdbc.MySQLProfile
    }
}

//via ssh tunnel
songanize-prod = {
    dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
    properties = {
        url = "jdbc:mysql://127.0.0.1:3306/c1songbase?user=c1songdbuser&password=changeme&serverTimezone=Europe/Zurich"
        driver = slick.jdbc.MySQLProfile
    }
}

dockerdefault = ${scotty-mole-integration-testing}
