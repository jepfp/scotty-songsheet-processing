scotty-mole-integration-testing-dbsetup = {
  url = "jdbc:mysql://localhost:3306/?user=root&password=enterPasswordHere"
  driver = slick.driver.MySQLDriver
  connectionPool = disabled
  keepAliveConnection = true
}

scotty-mole-integration-testing = {
  url = "jdbc:mysql://localhost:3306/scottymole_inttest?user=root&password=enterPasswordHere"
  driver = slick.driver.MySQLDriver
  connectionPool = disabled
  keepAliveConnection = true
}
